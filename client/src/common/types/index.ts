export type CategoryType = {
    label: string;
    index: number;
}

export type RecentPostType = {
    platform: string;
    name: string;
    date: Date;
}

export type NewsLetterType = {
    email: string;
}